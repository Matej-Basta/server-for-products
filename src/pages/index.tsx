import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

export default function Home() {
  return (
   <div>This is just to see, that everything works fine.</div>
  );
}